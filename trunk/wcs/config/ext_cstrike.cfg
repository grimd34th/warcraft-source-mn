
// XP gained for rescuing a hostage
   wcs_cfg_hostagexp 10

// XP gained for defusing the bomb
   wcs_cfg_defusexp 30

// XP gained for planting the bomb
   wcs_cfg_plantxp 15

// XP gained for making the bomb explode
   wcs_cfg_explodexp 15

// Allow the hostage_rescued event for races and items (1 = ON, 0 = OFF)
   wcs_cfg_allow_hostage_rescued 0

// Allow the bomb_defused event for races and items (1 = ON, 0 = OFF)
   wcs_cfg_allow_bomb_defused 0

// Allow the bomb_planted event for races and items (1 = ON, 0 = OFF)
   wcs_cfg_allow_bomb_planted 0

// Allow the bomb_exploded event for races and items (1 = ON, 0 = OFF)
   wcs_cfg_allow_bomb_exploded 0

// Allow the player_flashexplode event for races and items (1 = ON, 0 = OFF)
   wcs_cfg_allow_player_flashexplode 0

// Allow the player_heexplode event for races and items (1 = ON, 0 = OFF)
   wcs_cfg_allow_player_heexplode 0

// Allow the player_smokeexplode event for races and items (1 = ON, 0 = OFF)
   wcs_cfg_allow_player_smokeexplode 0
